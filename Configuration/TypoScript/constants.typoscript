
plugin.tx_lbolinks {
    view {
        # cat=plugin.tx_lbolinks/file; type=string; label=Path to template root (FE)
        templateRootPath =
        # cat=plugin.tx_lbolinks/file; type=string; label=Path to template partials (FE)
        partialRootPath =
        # cat=plugin.tx_lbolinks/file; type=string; label=Path to template layouts (FE)
        layoutRootPath =
    }
    settings {
        siteAssets =
        dateFormat =
    }
}
